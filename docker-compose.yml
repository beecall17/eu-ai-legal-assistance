version: '3.8'
services:
  legal-assistant:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env   # loads all env vars from .env
    volumes:
      - ./data:/app/data   # for ChromaDB
    restart: unless-stopped